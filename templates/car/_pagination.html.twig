<!-- Pagination -->
<nav aria-label="Page navigation example">
    <ul class="pagination justify-content-center mt-5">
        <!-- Previous button -->
        {% if currentPage > 1 %}
        <li class="page-item">
            <a class="page-link" href="{{ path( "app_car_index", {page: currentPage - 1}) }}" role="button"
                aria-pressed="true">Previous</a>
        </li>
        {% else %}
        <li class="page-item disabled">
            <a class="page-link" href="{{ path( "app_car_index", {page: currentPage - 1}) }}" tabindex="-1"
                role="button" aria-disabled="true">Previous</a>
        </li>
        {% endif %}

        <!-- Page button -->
        {% if totalPages >= 1  %}
            {% for i in 1.. totalPages %}
                {% if searchData is defined %}
                <li class="page-item"><a class="page-link" href="?data={{ searchData }}&page={{ i }}">{{ i }}</a></li>
                {% else %}
                <li class="page-item"><a class="page-link" href="?page={{ i }}">{{ i }}</a></li>
                {% endif %}
            {% endfor %}    
        {% endif %}

        <!-- Next button -->
        {% if currentPage < totalPages %} <li class="page-item">
            <a class="page-link" href="{{ path( "app_car_index", {page: currentPage + 1}) }}" role="button"
                aria-pressed="true">Next</a>
            </li>
        {% else %}
            <li class="page-item disabled">
                <a class="page-link" href="{{ path( "app_car_index", {page: currentPage + 1}) }}" tabindex="-1"
                    role="button" aria-disabled="true">Next</a>
            </li>
        {% endif %}
    </ul>
</nav>